# Gemini Notes

`GEMINI_CHANGES.diff` is the authoritative record of the previous large renderer/gameplay pass.

Its work was partially integrated and is now reflected in the current codebase, but the diff should still be treated as historical context rather than current state. Use `PROJECT_STATE.md`, `ROADMAP.md`, `NEXT_STEPS.md`, and `HANDOFF.md` for the verified current status.
